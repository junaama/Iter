# 080 - macOS workspace polish

## Task

Polish the macOS workspace shell so Team and Sessions no longer render placeholder pages, the titlebar uses native macOS controls only, the profile identity is user-friendly, and the UI follows the design prototype more closely without relying on broad mock data.

## Acceptance

- Team and Sessions render useful app surfaces instead of "Not implemented yet."
- The app does not render fake close/minimize/expand controls.
- Profile identity is presented through a dropdown and avoids raw user UUIDs.
- Changes stay aligned with the prototype and existing Iter design docs.
