package main

import "fmt"

// Compute the nth Fibonacci number iteratively.
func fib(n int) int {
	a, b := 0, 1
	for i := 0; i < n; i++ {
		a, b = b, a+b
	}
	return a
}

func main() {
	for i := 0; i < 10; i++ {
		fmt.Println(i, fib(i))
	}
}
